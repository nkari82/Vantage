{
  "name": "vantage-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app/backend",
    "app/dashboard"
  ],
  "scripts": {
    "build": "npm run build -w app/backend && npm run build -w app/dashboard",
    "dev:backend": "npm run dev -w app/backend",
    "dev:dashboard": "npm run dev -w app/dashboard"
  }
}
